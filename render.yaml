services:
  - type: web
    name: ip-quiz-app
    env: java
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: chmod +x start.sh && ./start.sh
    envVars:
      - key: PORT
        value: 10000